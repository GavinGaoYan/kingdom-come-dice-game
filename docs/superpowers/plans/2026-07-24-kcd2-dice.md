# KCD2 Dice Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Ship a from-scratch KCD2 dice game (scoring + match + AI + Tk UI) with tests.

**Architecture:** Pure `scoring` → `game` state machine → `ai` policy → Tk `ui`. Entrypoint `main.py`.

**Tech Stack:** Python 3.10+, tkinter (stdlib), pytest.

---

### Task 1: Scoring engine (TDD)

**Files:** `kcd2_dice/scoring.py`, `tests/test_scoring.py`

- Write failing tests for the scoring table + multi-combo + invalid leftovers
- Implement `score_selection`, `has_any_score`
- Run pytest until green
- Commit

### Task 2: Game state machine (TDD)

**Files:** `kcd2_dice/game.py`, `tests/test_game.py`

- Model match/turn state + actions (roll, toggle, continue, pass)
- Cover bust, bank, hot dice, win
- Commit

### Task 3: AI policy

**Files:** `kcd2_dice/ai.py`, `tests/test_ai.py`

- Selection + continue/pass heuristics
- Deterministic tests with seeded RNG where needed
- Commit

### Task 4: Tk UI + entrypoint

**Files:** `kcd2_dice/ui.py`, `main.py`, `README.md`

- Wire UI to game; AI via `after`
- Remove old monolith files
- Manual smoke: `python main.py`
- Commit

### Task 5: Verify + PR

- Full pytest
- Push branch, open draft PR
